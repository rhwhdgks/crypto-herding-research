# Invalidated experiment family

These scripts and their outputs depend on the pre-v2 `micro_herding_up/down` semantics. They are retained only for audit history and must not be used to support an alpha, p-value, or execution claim. Rebuild the analysis from schema-v2 micro frames, train-fitted threshold artifacts, selection-aware permutation, and complete-basket execution accounting.
