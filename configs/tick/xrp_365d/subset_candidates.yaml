input:
  base_dir: outputs/v2/tick/xrp_365d/cost_sanity

analysis:
  holdout_days_list:
    - 30
    - 60
  round_trip_cost_bps: 4
  block_days: 30

output:
  base_dir: outputs/v2/tick/xrp_365d/subset_candidates

logging:
  level: INFO
