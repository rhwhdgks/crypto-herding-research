input:
  base_dir: outputs/v2/tick/xrp_365d/short_horizon

rule:
  interval_minutes: 15
  event_label: up
  horizon_minutes: 30
  holdout_days: 30
  symbols:
    - XRPUSDT

output:
  base_dir: outputs/v2/tick/xrp_365d/fixed_rule/oos

logging:
  level: INFO
